name: graphql_client_repository
description: A Flutter package that provides a wrapper for GraphQL client to be used in repositories, simplifying GraphQL integration in Flutter applications.
version: 1.0.1
publish_to: 

environment:
  sdk: ">=3.1.2 <4.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  gql: ^1.0.1-alpha+1730759315362
  graphql: ^5.1.3
  http: ^1.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0